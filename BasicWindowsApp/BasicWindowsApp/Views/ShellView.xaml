<metro:MetroWindow x:Class="BasicWindowsApp.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:constants="clr-namespace:BasicWindowsApp.Constants"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:BasicWindowsApp.Views"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignPaper}"
        Title="{Binding ShellTitle}"
        TitleCharacterCasing="Normal"
        Height="450" Width="800">
    <DockPanel>
        <ContentControl VerticalAlignment="Stretch"
                        HorizontalAlignment="Stretch"
                        prism:RegionManager.RegionManager="{Binding AppRegionManager, Mode=OneWay}"
                        prism:RegionManager.RegionName="{x:Static constants:Regions.MainRegion}" />
    </DockPanel>
</metro:MetroWindow>
